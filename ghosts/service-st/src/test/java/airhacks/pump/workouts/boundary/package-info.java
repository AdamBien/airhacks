/// System tests for REST resources using MicroProfile REST clients.
///
/// The package structure mirrors the service module. `src/test/java` holds the
/// tests, `src/main/java` the REST client interfaces. Test classes are named
/// after the resource under test, with the `IT` suffix.
package airhacks.pump.workouts.boundary;
