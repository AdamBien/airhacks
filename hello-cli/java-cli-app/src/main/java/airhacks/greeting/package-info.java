/**
 * The {@code greeting} business component — owns the single job of producing a
 * friendly, human-facing message.
 * <p>
 * Its logic lives in the {@code control} layer
 * ({@link airhacks.greeting.control.Greetings}); this component needs no
 * {@code boundary} or {@code entity} layer.
 */
package airhacks.greeting;
