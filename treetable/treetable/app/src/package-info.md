# treetable
> A standards-based TreeTable — a table with nested rows — shipped as a single-page application.

## Vision
- A JTreeTable-class component built from nothing but web standards — hierarchy and attributes visible, and editable, in one place.

## Stack
- web-components · source root `app/src`
