package airhacks.qmp.transactions.entity;

/**
 * Status of a financial transaction.
 * Tracks the lifecycle state of transaction processing.
 */
public enum TransactionStatus {
    COMPLETED, FAILED, PENDING
}
