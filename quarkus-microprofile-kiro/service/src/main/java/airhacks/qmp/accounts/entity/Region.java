package airhacks.qmp.accounts.entity;

/**
 * Geographic regions determining account identification format.
 * Each region uses a different account identifier system.
 */
public enum Region {
    EUROPE, INDIA, US, ASIA_PACIFIC
}
