/// # contacts backend
/// > Store and serve contacts over HTTP for the contacts SPA.
///
/// ## Stack
/// - microprofile-server (Quarkus, Java 25) · base package `airhacks.contacts`
///
/// Cross-tier wiring and the assembly charter live in the frontend system doc
/// (`frontend/app/src/package-info.md`) — the calling side owns the dependency.
package airhacks.contacts;
