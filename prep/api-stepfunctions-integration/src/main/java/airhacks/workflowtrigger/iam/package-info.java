/**
 * Cross-service integration configuration for AWS service interoperability.
 * 
 * This component manages IAM roles and permissions required for direct service-to-service
 * communication, enabling serverless integrations between API Gateway, Step Functions,
 * and other AWS services without intermediate compute layers.
 */
package airhacks.workflowtrigger.iam;