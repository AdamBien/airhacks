package airhacks.qmp.accounts.entity;

/**
 * Supported currencies for account operations.
 * Based on ISO 4217 currency codes for major world currencies.
 */
public enum Currency {
    USD, EUR, GBP, JPY, CHF, CAD, AUD, CNY, INR, SGD
}
